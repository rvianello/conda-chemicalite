{% set version = "2020.11.0" %}

package:
  name: chemicalite
  version: {{ version }}

source:
  git_url: https://github.com/rvianello/chemicalite.git
  git_rev: {{ version }}

build:
  number: 0
  include_recipe: False

requirements:
  build:
    - cmake
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - sqlite {{ sqlite }}
    - rdkit {{ rdkit }}
    - eigen
  run:
    - sqlite
    - rdkit
